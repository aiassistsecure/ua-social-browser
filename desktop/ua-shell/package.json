{
  "name": "@workspace/ua-shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node ./build.mjs",
    "start": "pnpm run build && electron .",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "electron": "44.1.1",
    "esbuild": "0.27.3",
    "tsx": "catalog:"
  }
}
